spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: statistics-service
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
  sleuth:
    sampler:
      probability: 1.0
    enabled: true
  zipkin:
    base-url: http://9.98.30.76:32411/
    service:
      name: ${spring.application.name}
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG
