spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
     active: native
  security:
    user:
      password: root

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.103.1.54:8761/eureka

